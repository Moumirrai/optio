# Optio

Image and video transcoding utility with GUI interface.

As of now, video conversion requires ffmpeg to be installed.

## Live Development

To run in live development mode, run `wails dev` in the project directory.

## Building

To build a redistributable, production mode package, use `wails build`.
